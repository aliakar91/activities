import 'package:flutter/cupertino.dart';

class AppDimens {

static const EdgeInsets homeEdgeInsets = EdgeInsets.all(12);
static const EdgeInsets paddingHome = EdgeInsets.all(8);



static const BorderRadius borderRadiusSmall = BorderRadius.all(Radius.circular(10));
static const BorderRadius borderRadiusMedium = BorderRadius.all(Radius.circular(20));
static const BorderRadius borderRadiusLarge = BorderRadius.all(Radius.circular(30));


}